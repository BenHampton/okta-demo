server:
  port: 8080

spring:
  application:
    name: api-okta-demo-service
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: ${spring.security.oauth2.resourceserver.jwt.issuer-uri}

